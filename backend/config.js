/*
 * for default values
 *
 */

// DB connection
const host = 'localhost'
const user = 'koreahack'
const password = 'koreahack1234'
const database = 'koreahack-db'

module.exports = {
    host,
    user,
    password,
    database,
}