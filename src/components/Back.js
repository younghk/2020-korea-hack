import React, { useState, useEffect } from "react";
import styled, { css } from "styled-components";
import { Link } from "react-router-dom";

const Back = () => {
  return (
      <>
    <BackButton>
      <img src={require("../img/ic_arrow_left.png")} alt="img" />
    </BackButton>
    </>
  );
};

const BackButton 

export default Back;
